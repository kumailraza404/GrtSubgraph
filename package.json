{
  "name": "grt_token",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ kumailraza404/grt_token",
    "create-local": "graph create --node http://localhost:8020/ kumailraza404/grt_token",
    "remove-local": "graph remove --node http://localhost:8020/ kumailraza404/grt_token",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 kumailraza404/grt_token"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.25.1",
    "@graphprotocol/graph-ts": "0.24.1"
  }
}
